{
  "schemaVersion": 1,
  "id": "regenerated",
  "version": "${mod_version}",
  "icon": "${logo_file}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "MianReplicate"
  ],
  "contact": {
    "repo": "${repository_url}",
    "issues": "${issue_tracker_url}"
  },
  "license": "${mod_license}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "mc.jeryn.regenerated.fabric.RegeneratedFabric"
    ],
    "client": [
      "mc.jeryn.regenerated.fabric.RegeneratedFabricClient"
    ]
  },
  "mixins": [
    "${mod_id}-common.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric": ">=${fabric_api_version}",
    "minecraft": ">=${minecraft_version}"
  }
}
